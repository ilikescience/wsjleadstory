extends ../layouts/default

include ../components/card
include ../components/strap
include ../components/column

block content
    - var ahed = locals.articles.filter((article) => article.flashline === 'The A-hed' || article.flashline === 'The A-Hed')
    - var business = locals.articles.filter((article) => article.category === 'Business')
    - var life = locals.articles.filter((article) => article.storyType === 'Life')
    .l--grid.l--margin-top-xl
        .l--grid-row
            +strap('Spotlight Seven')
        .l--grid-row
            +column({
                span: 3,
                borders: {
                    right: 'light'
                },
                justified: true
            })
                +card(locals.articles[0], {
                    size: 'xl',
                    image: false,
                    actions: false,
                })
                +card(locals.articles[1], {
                    actions: false,
                    image: false,
                    bullets: [
                        {
                            title: 'Stocks Drop as Currency Crisis Roils Turkey'
                        },
                        {
                            title: 'Why Turkey’s Trials Ripple Into Europe'
                        }
                    ]
                })
                +card(locals.articles[2], {
                    actions: false,
                    image: false
                })
                +card(locals.articles[3], {
                    actions: false,
                    image: false,
                    borders: false
                })
            
            +column({span: 5})
                +card(locals.articles[4], {
                    size: 'l',
                    image: false,
                    actions: false,
                    bullets: [
                        {
                            title: 'For Uber and Airbnb, New York City Turns Foe'
                        },
                        {
                            title: 'New York’s Ride-Hailing Battleground Moves to the Outer Boroughs'
                        }
                    ]
                })
                +card(locals.articles[5], {
                    size: 'l'
                })
                +card(locals.articles[6], {
                    actions: false,
                    image: false,
                    borders: false,
                    bullets: [
                        {
                            title: 'Monsanto Hit by $289 Million Verdict in Cancer Case'
                        }
                    ]
                })

            +column({
                span: 2,
                borders: {
                    left: 'light'
                },
                justified: true
            })
                +card(locals.articles[7], {
                    summary: false,
                    byline: false,
                    actions: false,
                    timestamp: false,
                    borders: false
                })
                +card(locals.articles[8], {
                    summary: false,
                    byline: false,
                    actions: false,
                    timestamp: false,
                    borders: false
                })
                +card(locals.articles[9], {
                    summary: false,
                    byline: false,
                    actions: false,
                    timestamp: false,
                    borders: false
                })
                +card(locals.articles[10], {
                    summary: false,
                    byline: false,
                    actions: false,
                    timestamp: false,
                    borders: false
                })
    .l--grid.l--margin-top-xl
        .l--grid-row
            +strap('Nested grids')
        .l--grid-row
            +column({span: 2})
                +card(locals.articles[0], {
                    size: 'l'
                })
                .l--grid
                    .l--grid-row
                        +column()
                            +card(locals.articles[1], {
                                image: false,
                                actions: false
                            })
                        +column()
                            +card(locals.articles[2], {
                                image: false,
                                actions: false
                            })
            +column()
                +card(locals.articles[3], {
                    actions: false
                })
                +card(locals.articles[4], {
                    actions: false
                })
    .l--grid.l--margin-top-xl
        .l--grid-row
            +strap('Business')
        .l--grid-row
            +column({span: 2})
                +card(business[0], {
                    size: 'l'
                })
            +column()
                +card(business[1], {
                    actions: false,
                    image: false,
                    byline: false
                })
                +card(business[2], {
                    actions: false,
                    image: false,
                    byline: false
                })
                +card(business[3], {
                    actions: false,
                    image: false,
                    byline: false
                })
    .l--grid.l--margin-top-xl
        .l--grid-row
            +strap('Enterprise')
        .l--grid-row
            +column({span: 3})
                +card(ahed[0], {
                    alignment: 'center',
                    size: 'l',
                    ornament: 'ahed',
                    mediaPlacement: 'last',
                    image: 'https://d3vv6lp55qjaqc.cloudfront.net/items/0O0I0X3C2U2T1W2Z0d35/Screen%20Shot%202018-08-13%20at%209.03.00%20AM.png'
                })
            +column({span: 4})
                +card(life[1], {
                    size: 'xl',
                    alignment: 'center',
                    ornament: 'leder'
                })
    .l--grid.l--margin-top-xl
        .l--grid-row
            +strap('Overlays')
        .l--grid-row
            +column()
                +card(life[0], {
                    colorTheme: 'inverted',
                    ornament: 'leder',
                    alignment: 'center',
                    mediaPlacement: 'behind',
                    size: 'l',
                    byline: false,
                    timestamp: false,
                    actions: false
                })
                .l--grid
                    .l--grid-row
                        +column()
                            +card(life[1], {
                                colorTheme: 'inverted',
                                mediaPlacement: 'behind',
                                mediaRatio: 0.66,
                                actions: false,
                                summary: false
                            })
                        +column()
                            +card(life[2], {
                                colorTheme: 'inverted',
                                mediaPlacement: 'behind',
                                mediaRatio: 0.66,
                                actions: false,
                                summary: false
                            })
            +column()
                +card(life[3], {
                    mediaPlacement: 'behind',
                    size: 'xl',
                    mediaRatio: 1,
                    ornament: 'leder',
                    alignment: 'center',
                    headlinePosition: 'bottom',
                    actions: false
                })
                +card(life[4], {
                    colorTheme: 'inverted',
                    mediaPlacement: 'behind',
                    size: 'l',
                    mediaRatio: 1.66,
                    byline: false,
                    timestamp: false,
                    actions: false
                })
    .c--bg-jet
        .l--grid.l--margin-top-xl.l--padding-top-xl
            .l--grid-row
                +column()
                    +card(locals.articles[10], {
                        colorTheme: 'inverted',
                        actions: false,
                        orientation: 'horizontal'
                    })
                    +card(locals.articles[11], {
                        colorTheme: 'inverted',
                        size: 'l',
                        actions: false
                    })
                    +card(locals.articles[12], {
                        colorTheme: 'inverted',
                        size: 'l',
                        actions: false
                    })
                +column()
                    +card(locals.articles[13], {
                        colorTheme: 'inverted',
                        size: 'l',
                        actions: false
                    })
                    +card(locals.articles[14], {
                        colorTheme: 'inverted',
                        size: 'l',
                        actions: false
                    })
                    +card(locals.articles[15], {
                        colorTheme: 'inverted',
                        size: 'l',
                        actions: false
                    })